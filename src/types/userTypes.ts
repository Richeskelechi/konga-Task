// types.ts

export interface UserData {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    role: string;
}
export interface UserDataLogin {
    firstName: string;
    lastName: string;
    email: string;
    password: string;
    role: string;
}
